/**
 * Reusable CSS token strings for Recharts / canvas (match `src/styles/tailwind.css`).
 */
export const CHART_AXIS_STROKE = "hsl(var(--muted-foreground))";
export const CHART_GRID_STROKE = "hsl(var(--border))";

export const CHART_SERIES = [
  "hsl(var(--chart-1))",
  "hsl(var(--chart-2))",
  "hsl(var(--chart-3))",
  "hsl(var(--chart-4))",
  "hsl(var(--chart-5))",
] as const;
