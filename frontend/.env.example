VITE_API_URL=http://127.0.0.1:8000
# Optional: public browser URL for the SPA (for documentation / deep links)
# VITE_PUBLIC_APP_URL=http://localhost:5173

# Light/dark UI theme is chosen in the app header (sun/moon or Profile → Appearance).
# Persisted in the browser as localStorage key "pp-theme" (values: light | dark; omit for system default).
