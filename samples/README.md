Sample inputs for local testing.

- `weekly_history.csv` — ready to upload with the UI or API.
- Status tracker and RAID log must be Excel workbooks whose columns match `backend/app/constants/columns.py` (see repository README).
